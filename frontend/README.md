# Frontend

`public/` contains the existing todo app, kept intact during the initial folder split.
It currently uses plain HTML, CSS, and JavaScript, and saves data in the browser's
`localStorage`. The next step is to migrate its UI to React + Vite, preserving its
existing features, then connect it to the backend API.
