# Backend

This is the initial Express API scaffold. Run `npm install`, then `npm run dev`.
The `/api/health` endpoint confirms that the server is running. Todo data is not
connected yet; the next step is adding a database and CRUD API before wiring the
frontend to it.
